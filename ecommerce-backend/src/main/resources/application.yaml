spring:
  application:
    name: ecommerce-poc
  security:
    user:
      name: none
      password: none
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_db
    username: root
    password: password
    jpa:
      hibernate:
        ddl-auto: validate
      show-sql: true
      properties:
        hibernate:
          format_sql: true
  elasticsearch:
    uris: http://localhost:9200
server:
  port: 8080